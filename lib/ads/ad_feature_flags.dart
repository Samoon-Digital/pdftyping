/// Global ad-format feature switches.
///
/// Native Ads are intentionally disabled app-wide. Keep this false to ensure
/// no Native Ad requests, preloads, renders, skeletons, or reserved gaps run in
/// debug or release builds.
const bool nativeAdsEnabled = false;
